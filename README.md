# Smart-cafe-ordering-system
The SmartCafe ordering system is a simulaƟon of the digital order processing in a cafe where
the site and the client communicate through the client-server socket in Python [1]. The
systems are made up of three modules:
 Meny.py:
stores the menu (dicƟonary) in one locaƟon and displays it in another locaƟon (in this
case, a separate files).
 Customer.py:
The structure of the program customer.py is that it takes the user input and places the
order in the kitchen by means of the sockets.
 kitchen.py:
Receive, process, and acknowledgement of orders with a simulated delay.
